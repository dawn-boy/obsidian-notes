# 1. Calculating the Sum to find the missing number instead of recursively comparing each elements

$$(n * (n + 1)) / 2 $$
calculates the sum of the first n natural numbers. Subtract that with the sum from the list. We get the missing number.

